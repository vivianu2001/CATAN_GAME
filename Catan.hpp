#ifndef BOARD_HPP
#define BOARD_HPP

#include <vector>
#include "Tile.hpp"

class Board {
public:
    std::vector<Tile> tiles;

    Board();
    // Add methods for managing the board if needed
};

#endif // BOARD_HPP
